# syntax=docker/dockerfile:1

FROM ghcr.io/searxng/searxng:latest

# Copy SearxNG configuration into the image
COPY searxng /etc/searxng

EXPOSE 8080

# Default command provided by base image runs SearxNG
